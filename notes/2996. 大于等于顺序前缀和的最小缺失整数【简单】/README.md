# [2996. 大于等于顺序前缀和的最小缺失整数【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/2996.%20%E5%A4%A7%E4%BA%8E%E7%AD%89%E4%BA%8E%E9%A1%BA%E5%BA%8F%E5%89%8D%E7%BC%80%E5%92%8C%E7%9A%84%E6%9C%80%E5%B0%8F%E7%BC%BA%E5%A4%B1%E6%95%B4%E6%95%B0%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 暴力解法](#2--s1---暴力解法)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/smallest-missing-integer-greater-than-sequential-prefix-sum/)

给你一个下标从 0 开始的整数数组 `nums`。

如果一个前缀 `nums[0..i]` 满足对于 `1 <= j <= i` 的所有元素都有 `nums[j] = nums[j - 1] + 1`，那么我们称这个前缀是一个顺序前缀。特殊情况是，只包含 `nums[0]` 的前缀也是一个顺序前缀。

请你返回 `nums` 中没有出现过的最小整数 `x`，满足 `x` 大于等于最长顺序前缀的和。

---

示例 1：

```txt
输入：nums = [1,2,3,2,5]
输出：6
```

解释：nums 的最长顺序前缀是 [1,2,3]，和为 6，6 不在数组中，所以 6 是大于等于最长顺序前缀和的最小整数。

---

示例 2：

```txt
输入：nums = [3,4,5,1,12,14,13]
输出：15
```

解释：nums 的最长顺序前缀是 [3,4,5]，和为 12，12、13 和 14 都在数组中，但 15 不在，所以 15 是大于等于最长顺序前缀和的最小整数。

---

提示：

- `1 <= nums.length <= 50`
- `1 <= nums[i] <= 50`

## 2. 🎯 s.1 - 暴力解法

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(n)$，遍历数组计算前缀和和构建哈希表
- 空间复杂度：$O(n)$，哈希表存储所有元素

算法思路：

- 先计算最长顺序前缀的和：从第一个元素开始，只要后一个元素等于前一个元素加 1，就累加到和中
- 用哈希表存储数组中的所有数字
- 从前缀和开始，找第一个不在数组中出现的数字
