---
id: e0b128bb-d1e7-4bfd-b4a8-ed1d7ffa51b5
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/apply-operations-to-make-all-array-elements-equal-to-zero/)

给你一个下标从 0 开始的整数数组 `nums` 和一个正整数 `k`。

你可以对数组执行下述操作 任意次 ：

- 从数组中选出长度为 `k` 的 任一 子数组，并将子数组中每个元素都 减去 `1`。

如果你可以使数组中的所有元素都等于 `0`，返回 `true` ；否则，返回 `false`。

子数组 是数组中的一个非空连续元素序列。

---

示例 1：

```txt
输入：nums = [2,2,3,1,1,0], k = 3
输出：true
解释：可以执行下述操作：
- 选出子数组 [2,2,3]，执行操作后，数组变为 nums = [1,1,2,1,1,0]。
- 选出子数组 [2,1,1]，执行操作后，数组变为 nums = [1,1,1,0,0,0]。
- 选出子数组 [1,1,1]，执行操作后，数组变为 nums = [0,0,0,0,0,0]。
```

示例 2：

```txt
输入：nums = [1,3,1,1], k = 2
输出：false
解释：无法使数组中的所有元素等于 0。
```

---

提示：

- `1 <= k <= nums.length <= 10^5`
- `0 <= nums[i] <= 10^6`

## 2. 🎯 s.1 - 差分数组

::: code-group

```js [js]
/**
 * @param {number[]} nums
 * @param {number} k
 * @return {boolean}
 */
var checkArray = function (nums, k) {
  const n = nums.length
  const diff = new Array(n + 1).fill(0) // 差分数组
  let cur = 0
  for (let i = 0; i < n; i++) {
    cur += diff[i]
    const val = nums[i] + cur
    if (val < 0) return false
    if (val === 0) continue
    if (i + k > n) return false
    cur -= val
    diff[i + k] += val
  }
  return true
}
```

```c [c]
bool checkArray(int *nums, int numsSize, int k) {
    int n = numsSize;
    long long *diff = (long long *)calloc(n + 1, sizeof(long long));
    long long cur = 0;
    for (int i = 0; i < n; i++) {
        cur += diff[i];
        long long val = nums[i] + cur;
        if (val < 0) { free(diff); return false; }
        if (val == 0) continue;
        if (i + k > n) { free(diff); return false; }
        cur -= val;
        diff[i + k] += val;
    }
    free(diff);
    return true;
}
```

```py [py]
class Solution:
    def checkArray(self, nums: list[int], k: int) -> bool:
        n = len(nums)
        diff = [0] * (n + 1)
        cur = 0
        for i in range(n):
            cur += diff[i]
            val = nums[i] + cur
            if val < 0:
                return False
            if val == 0:
                continue
            if i + k > n:
                return False
            cur -= val
            diff[i + k] += val
        return True
```

:::

- 时间复杂度：$O(n)$，遍历一次数组
- 空间复杂度：$O(n)$，差分数组的空间

算法思路：

- 从左到右遍历，若当前位置值 `val > 0`，则对 `[i, i+k-1]` 范围减去 `val`
- 用差分数组高效实现区间减法操作
- 若任何位置出现负值或剩余位置不足 k 个则返回 false
