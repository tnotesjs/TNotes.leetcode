# [2148. 元素计数【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/2148.%20%E5%85%83%E7%B4%A0%E8%AE%A1%E6%95%B0%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 统计边界](#2--s1---统计边界)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/count-elements-with-strictly-smaller-and-greater-elements/)

给你一个整数数组 `nums` ，统计并返回在 `nums` 中同时至少具有一个严格较小元素和一个严格较大元素的元素数目。

---

示例 1：

```txt
输入：nums = [11,7,2,15]
输出：2
解释：元素 7 ：严格较小元素是元素 2 ，严格较大元素是元素 11。
元素 11 ：严格较小元素是元素 7 ，严格较大元素是元素 15。
总计有 2 个元素都满足在 nums 中同时存在一个严格较小元素和一个严格较大元素。
```

示例 2：

```txt
输入：nums = [-3,3,3,90]
输出：2
解释：元素 3 ：严格较小元素是元素 -3 ，严格较大元素是元素 90。
由于有两个元素的值为 3 ，总计有 2 个元素都满足在 nums 中同时存在一个严格较小元素和一个严格较大元素。
```

---

提示：

- `1 <= nums.length <= 100`
- `-10^5 <= nums[i] <= 10^5`

## 2. 🎯 s.1 - 统计边界

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(N)$，单次遍历统计最小/最大及其出现次数
- 空间复杂度：$O(1)$，仅使用常数计数变量

解题思路：

- 先找到最小值和最大值并统计它们的出现次数，若两者相等说明全数组同值返回 0；否则答案为 `n - minCnt - maxCnt`，即排除等于最小或最大值的元素数量。
