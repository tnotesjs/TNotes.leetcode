# [0027. 移除元素【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/0027.%20%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 双指针（1）](#2--s1---双指针1)
- [3. 🎯 s.2 - 双指针（2）](#3--s2---双指针2)
- [4. 🎯 s.3 - 双指针（3）](#4--s3---双指针3)
- [5. 🎯 s.4 - 调用系统函数暴力处理](#5--s4---调用系统函数暴力处理)
- [6. 🔗 引用](#6--引用)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/remove-element/)

给你一个数组 `nums` 和一个值 `val`，你需要 [原地][1] 移除所有数值等于 `val` 的元素。元素的顺序可能发生改变。然后返回 `nums` 中与 `val` 不同的元素的数量。

假设 `nums` 中不等于 `val` 的元素数量为 `k`，要通过此题，您需要执行以下操作：

- 更改 `nums` 数组，使 `nums` 的前 `k` 个元素包含不等于 `val` 的元素。`nums` 的其余元素和 `nums` 的大小并不重要。
- 返回 `k`。

用户评测：

评测机将使用以下代码测试您的解决方案：

```txt
int[] nums = [...]; // 输入数组
int val = ...; // 要移除的值
int[] expectedNums = [...]; // 长度正确的预期答案。
                            // 它以不等于 val 的值排序。

int k = removeElement(nums, val); // 调用你的实现

assert k == expectedNums.length;
sort(nums, 0, k); // 排序 nums 的前 k 个元素
for (int i = 0; i < actualLength; i++) {
    assert nums[i] == expectedNums[i];
}
```

如果所有的断言都通过，你的解决方案将会 通过。

示例 1：

```txt
输入：nums = [3,2,2,3], val = 3
输出：2, nums = [2,2,_,_]
解释：你的函数应该返回 k = 2, 并且 nums 中的前两个元素均为 2。
你在返回的 k 个元素之外留下了什么并不重要（因此它们并不计入评测）。
```

示例 2：

```txt
输入：nums = [0,1,2,2,3,0,4,2], val = 2
输出：5, nums = [0,1,4,0,3,_,_,_]
解释：你的函数应该返回 k = 5，并且 nums 中的前五个元素为 0,0,1,3,4。
注意这五个元素可以任意顺序返回。
你在返回的 k 个元素之外留下了什么并不重要（因此它们并不计入评测）。
```

提示：

- `0 <= nums.length <= 100`
- `0 <= nums[i] <= 50`
- `0 <= val <= 100`

## 2. 🎯 s.1 - 双指针（1）

::: code-group

<<< ./solutions/1/1.js

<<< ./solutions/1/2.js

:::

- 时间复杂度：$O(n^2)$，其中 n 是数组长度，最坏情况下每个元素都需要查找后续元素进行交换
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

通过双层循环实现原地移除。外层遍历数组，遇到需要移除的元素时，内层从后续查找不等于目标值的元素进行交换。做法 2 在做法 1 的基础上做了适当的优化，在内层扫描中，如果发现后续的所有成员都是需要移除的，直接结束后续不必要的检查流程。

::: swiper

![1](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-11-27-12-13-35.png)

![2](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-11-27-12-13-28.png)

:::

## 3. 🎯 s.2 - 双指针（2）

::: code-group

<<< ./solutions/2/1.js

:::

- 时间复杂度：$O(n)$，其中 n 是数组长度，每个元素最多被访问一次
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

使用双指针（对撞指针）实现。左指针从左向右查找等于目标值的元素，右指针从右向左查找不等于目标值的元素，找到后交换两个位置的值。重复此过程直到两指针相遇。注意内层两个 while 循环的限制条件 `l < r` 不能少，防止两指针相撞之后继续运动。由于循环结束分两种情况（左指针撞向右指针或右指针撞向左指针），所以最终返回的新数组长度也需要分情况判断。

## 4. 🎯 s.3 - 双指针（3）

::: code-group

<<< ./solutions/3/1.js

:::

- 时间复杂度：$O(n)$，其中 n 是数组长度，只需遍历数组一次
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

使用快慢双指针实现。快指针 `fast` 遍历数组，慢指针 `slow` 指向下一个要存放非目标值的位置。当快指针遇到等于目标值的元素时，直接跳过；遇到不等于目标值的元素时，将其赋值给慢指针位置，然后两个指针都向前移动。最后慢指针的位置加 1 即为新数组的长度。这种方法相比对撞指针更加简洁高效。

## 5. 🎯 s.4 - 调用系统函数暴力处理

::: code-group

<<< ./solutions/4/1.js

:::

- 时间复杂度：$O(n^2)$，其中 n 是数组长度，splice 操作本身是 $O(n)$，最坏情况下需要执行 n 次
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

调用 JavaScript 原生 API `splice` 实现。遍历数组，一旦找到等于目标值的元素，直接用 `splice` 从原数组中删除。由于 `splice` 会改变原数组，删除元素后需要将索引 `i` 减 1，以避免跳过元素。最后返回修改后的数组长度即可。注意：这种方法虽然能通过测试，但 `splice` 操作涉及数组元素的移动，性能不如双指针方法。

## 6. 🔗 引用

- [百度百科 - 原地算法][1]
- [Bilibili - 爱学习的饲养员 Leetcode 力扣 1-300 题视频讲解合集｜手画图解版+代码【持续更新 ing】][2]

[1]: https://baike.baidu.com/item/%E5%8E%9F%E5%9C%B0%E7%AE%97%E6%B3%95
[2]: https://www.bilibili.com/video/BV1xa411A76q?p=7
