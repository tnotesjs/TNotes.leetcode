# [0066. 加一【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/0066.%20%E5%8A%A0%E4%B8%80%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 从末尾逐位进位](#2--s1---从末尾逐位进位)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/plus-one/)

给定一个由整数组成的非空数组所表示的非负整数，在该数的基础上加一。

最高位数字存放在数组的首位，数组中每个元素只存储单个数字。

你可以假设除了整数 0 之外，这个整数不会以零开头。

---

示例 1：

```
输入：digits = [1, 2, 3]
输出：[1, 2, 4]
```

解释：输入数组表示数字 123。

---

示例 2：

```
输入：digits = [4, 3, 2, 1]
输出：[4, 3, 2, 2]
```

解释：输入数组表示数字 4321。

---

示例 3：

```
输入：digits = [9]
输出：[1, 0]
```

解释：

- 输入数组表示数字 9。
- 加 1 得到了 9 + 1 = 10。
- 因此，结果应该是 [1, 0]。

---

提示：

- `1 <= digits.length <= 100`
- `0 <= digits[i] <= 9`

## 2. 🎯 s.1 - 从末尾逐位进位

::: code-group

<<< ./solutions/1/1.c [c]

<<< ./solutions/1/1.js [js]

<<< ./solutions/1/1.py [py]

:::

- 时间复杂度：$O(n)$，其中 $n$ 是数组长度，最坏情况所有位都是 9 需要遍历整个数组
- 空间复杂度：$O(1)$，除了返回结果外只使用常数额外空间；若所有位均为 9 则返回长度为 $n+1$ 的新数组

算法思路：

- 从数组末尾开始逐位加一，模拟手动加法中的进位过程
- 若当前位加一后小于 10，说明没有进位，直接修改该位并返回原数组
- 若当前位加一后等于 10，将该位置为 0，继续向前进位
- 若遍历完所有位仍有进位（例如 `[9, 9, 9]`），则在数组开头插入 `1`，得到 `[1, 0, 0, 0]`
