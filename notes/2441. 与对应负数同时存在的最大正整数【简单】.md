---
id: 8d2fc005-8a55-457b-9a43-2f21dc8cac21
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/largest-positive-integer-that-exists-with-its-negative/)

给你一个 不包含 任何零的整数数组 `nums`，找出自身与对应的负数都在数组中存在的最大正整数 `k`。

返回正整数 `k`，如果不存在这样的整数，返回 `-1`。

---

示例 1：

```txt
输入：nums = [-1,2,-3,3]
输出：3
解释：3 是数组中唯一一个满足题目要求的 k。
```

---

示例 2：

```txt
输入：nums = [-1,10,6,7,-7,1]
输出：7
解释：数组中存在 1 和 7 对应的负数，7 的值更大。
```

---

示例 3：

```txt
输入：nums = [-10,8,6,7,-2,-3]
输出：-1
解释：不存在满足题目要求的 k，返回 -1。
```

---

提示：

- `1 <= nums.length <= 1000`
- `-1000 <= nums[i] <= 1000`
- `nums[i] != 0`

## 2. 🎯 s.1 - 哈希集合

::: code-group

```js [js]
/**
 * @param {number[]} nums
 * @return {number}
 */
var findMaxK = function (nums) {
  const set = new Set(nums)
  let maxK = -1

  for (const num of nums) {
    if (num > 0 && set.has(-num)) {
      maxK = Math.max(maxK, num)
    }
  }

  return maxK
}
```

:::

- 时间复杂度：$O(N)$，其中 N 是数组 nums 的长度
- 空间复杂度：$O(N)$，哈希集合存储所有元素

算法思路：

- 将数组所有元素存入哈希集合，便于快速查找
- 遍历数组，对于每个正整数，检查其对应的负数是否在集合中
- 记录满足条件的最大正整数
- 如果不存在这样的整数，返回 -1
