# [3196. 最大化子数组的总成本【中等】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/3196.%20%E6%9C%80%E5%A4%A7%E5%8C%96%E5%AD%90%E6%95%B0%E7%BB%84%E7%9A%84%E6%80%BB%E6%88%90%E6%9C%AC%E3%80%90%E4%B8%AD%E7%AD%89%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 解法 1](#2--s1---解法-1)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/maximize-total-cost-of-alternating-subarrays/)

给你一个长度为 `n` 的整数数组 `nums`。

子数组 `nums[l..r]`（其中 `0 <= l <= r < n`）的 成本 定义为：

`cost(l, r) = nums[l] - nums[l + 1] + ... + nums[r] * (−1)^r − l`

你的任务是将 `nums` 分割成若干子数组，使得所有子数组的成本之和 最大化，并确保每个元素 正好 属于一个子数组。

具体来说，如果 `nums` 被分割成 `k` 个子数组，且分割点为索引 `i1, i2, ..., ik − 1`（其中 `0 <= i1 < i2 < ... < ik - 1 < n - 1`），则总成本为：

`cost(0, i1) + cost(i1 + 1, i2) + ... + cost(ik − 1 + 1, n − 1)`

返回在最优分割方式下的子数组成本之和的最大值。

---

注意：如果 `nums` 没有被分割，即 `k = 1`，则总成本即为 `cost(0, n - 1)`。

---

示例 1：

输入：nums = [1,-2,3,4]

输出：10

解释：

一种总成本最大化的方法是将 `[1, -2, 3, 4]` 分割成子数组 `[1, -2, 3]` 和 `[4]`。总成本为 `(1 + 2 + 3) + 4 = 10`。

---

示例 2：

输入：nums = [1,-1,1,-1]

输出：4

解释：

一种总成本最大化的方法是将 `[1, -1, 1, -1]` 分割成子数组 `[1, -1]` 和 `[1, -1]`。总成本为 `(1 + 1) + (1 + 1) = 4`。

---

示例 3：

输入：nums = [0]

输出：0

解释：

无法进一步分割数组，因此答案为 0。

---

示例 4：

输入：nums = [1,-1]

输出：2

解释：

选择整个数组，总成本为 `1 + 1 = 2`，这是可能的最大成本。

---

提示：

- `1 <= nums.length <= 10^5`
- `-10^9 <= nums[i] <= 10^9`

## 2. 🎯 s.1 - 动态规划

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(n)$，遍历数组一次
- 空间复杂度：$O(1)$，只用常数额外空间

算法思路：

- 每个位置的元素要么带正号（子数组开头或偶数位），要么带负号（子数组奇数位）
- pos 表示当前位置带正号的最大值，neg 表示带负号的最大值
- 在任意位置都可以开启新子数组（强制正号），从而取 max(pos, neg) + nums[i]

