# [2516. 每种字符至少取 K 个【中等】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/2516.%20%E6%AF%8F%E7%A7%8D%E5%AD%97%E7%AC%A6%E8%87%B3%E5%B0%91%E5%8F%96%20K%20%E4%B8%AA%E3%80%90%E4%B8%AD%E7%AD%89%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 滑动窗口](#2--s1---滑动窗口)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/take-k-of-each-character-from-left-and-right/)

给你一个由字符 `'a'`、`'b'`、`'c'` 组成的字符串 `s` 和一个非负整数 `k`。每分钟，你可以选择取走 `s` 最左侧 还是 最右侧 的那个字符。

你必须取走每种字符 至少 `k` 个，返回需要的 最少 分钟数；如果无法取到，则返回 `-1`。

---

示例 1：

```txt
输入：s = "aabaaaacaabc", k = 2
输出：8
解释：
从 s 的左侧取三个字符，现在共取到两个字符 'a' 、一个字符 'b'。
从 s 的右侧取五个字符，现在共取到四个字符 'a' 、两个字符 'b' 和两个字符 'c'。
共需要 3 + 5 = 8 分钟。
可以证明需要的最少分钟数是 8。
```

示例 2：

```txt
输入：s = "a", k = 1
输出：-1
解释：无法取到一个字符 'b' 或者 'c'，所以返回 -1。
```

---

提示：

- `1 <= s.length <= 10^5`
- `s` 仅由字母 `'a'`、`'b'`、`'c'` 组成
- `0 <= k <= s.length`

## 2. 🎯 s.1 - 滑动窗口

::: code-group

<<< ./solutions/1/1.js [js] <<< ./solutions/1/1.c [c] <<< ./solutions/1/1.py [py]

:::

- 时间复杂度：$O(n)$，其中 $n$ 是字符串长度
- 空间复杂度：$O(1)$，只使用常数额外空间

算法思路：

- 问题等价于找最长的中间窗口，使得窗口外（左右两侧）每种字符至少 $k$ 个
- 加入窗口右端字符，若窗口外某字符不足 $k$，则收缩左端
- 答案为 $n - \text{maxWin}$（窗口外的总长度）
