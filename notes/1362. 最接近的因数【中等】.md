---
id: 16a3fdbd-c0b6-452b-99f8-5cbf131865b0
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/closest-divisors/)

给你一个整数 `num`，请你找出同时满足下面全部要求的两个整数：

- 两数乘积等于 `num + 1` 或 `num + 2`
- 以绝对差进行度量，两数大小最接近

你可以按任意顺序返回这两个整数。

---

示例 1：

```txt
输入：num = 8
输出：[3,3]

解释：
对于 num + 1 = 9，最接近的两个因数是 3 & 3；
对于 num + 2 = 10, 最接近的两个因数是 2 & 5，因此返回 3 & 3。
```

示例 2：

```txt
输入：num = 123
输出：[5,25]
```

示例 3：

```txt
输入：num = 999
输出：[40,25]
```

---

提示：

- `1 <= num <= 10^9`

## 2. 🎯 s.1 - 枚举因数

::: code-group

```js [js]
/**
 * @param {number} num
 * @return {number[]}
 */
var closestDivisors = function (num) {
  const find = (n) => {
    for (let i = Math.floor(Math.sqrt(n)); i >= 1; i--) {
      if (n % i === 0) return [i, n / i]
    }
    return [1, n]
  }
  const a = find(num + 1),
    b = find(num + 2)
  return Math.abs(a[0] - a[1]) <= Math.abs(b[0] - b[1]) ? a : b
}
```

```c [c]
int* closestDivisors(int num, int* returnSize) {
    *returnSize = 2;
    int* res = malloc(sizeof(int) * 2);
    int bestDiff = INT_MAX;
    for (int t = num + 1; t <= num + 2; t++) {
        for (int i = (int)sqrt(t); i >= 1; i--) {
            if (t % i == 0) {
                int diff = t / i - i;
                if (diff < bestDiff) {
                    bestDiff = diff;
                    res[0] = i;
                    res[1] = t / i;
                }
                break;
            }
        }
    }
    return res;
}
```

```py [py]
class Solution:
    def closestDivisors(self, num: int) -> list[int]:
        def find(n: int) -> list[int]:
            for i in range(int(n**0.5), 0, -1):
                if n % i == 0:
                    return [i, n // i]
            return [1, n]

        a, b = find(num + 1), find(num + 2)
        return a if abs(a[0] - a[1]) <= abs(b[0] - b[1]) else b
```

:::

- 时间复杂度：$O(\sqrt{num})$
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

算法思路：

- 分别对 num+1 和 num+2 找最接近平方根的因数对
- 从 sqrt(n) 向下枚举，找到第一个能整除的因数即为最接近的因数对
- 比较两个结果，返回差值更小的因数对
