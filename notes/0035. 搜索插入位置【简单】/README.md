# [0035. 搜索插入位置【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/0035.%20%E6%90%9C%E7%B4%A2%E6%8F%92%E5%85%A5%E4%BD%8D%E7%BD%AE%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 二分查找](#2--s1---二分查找)
- [3. 🎯 s.2 - 暴力解法](#3--s2---暴力解法)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/search-insert-position/)

给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。

请必须使用时间复杂度为 `O(log n)` 的算法。

示例 1:

```txt
输入: nums = [1,3,5,6], target = 5
输出: 2
```

示例 2:

```txt
输入: nums = [1,3,5,6], target = 2
输出: 1
```

示例 3:

```txt
输入: nums = [1,3,5,6], target = 7
输出: 4
```

提示:

- `1 <= nums.length <= 10^4`
- `-10^4 <= nums[i] <= 10^4`
- `nums` 为 无重复元素 的 升序 排列数组
- `-10^4 <= target <= 10^4`

## 2. 🎯 s.1 - 二分查找

::: code-group

<<< ./solutions/1/1.js

:::

- 时间复杂度：$O(\log n)$，其中 n 是数组长度，每次循环将搜索区间减半
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

使用二分查找在有序数组中查找目标值或插入位置。先处理目标值大于所有元素的特殊情况，然后通过不断缩小搜索区间找到第一个大于等于目标值的位置。

## 3. 🎯 s.2 - 暴力解法

::: code-group

<<< ./solutions/2/1.js

:::

- 时间复杂度：$O(n)$，其中 n 是数组长度，最坏情况下需要遍历整个数组
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

顺序遍历数组，找到第一个大于等于目标值的位置并返回。这种方法虽然简单直观，但时间复杂度为 $O(n)$，不满足题目要求的 $O(\log n)$。
