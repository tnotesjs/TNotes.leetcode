# [1309. 解码字母到整数映射【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/1309.%20%E8%A7%A3%E7%A0%81%E5%AD%97%E6%AF%8D%E5%88%B0%E6%95%B4%E6%95%B0%E6%98%A0%E5%B0%84%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 逆向扫描解码](#2--s1---逆向扫描解码)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/decrypt-string-from-alphabet-to-integer-mapping/)

给你一个字符串 `s`，它由数字（`'0'` - `'9'`）和 `'#'` 组成。我们希望按下述规则将 `s` 映射为一些小写英文字符：

- 字符（`'a'` - `'i'`）分别用（`'1'` - `'9'`）表示。
- 字符（`'j'` - `'z'`）分别用（`'10#'` - `'26#'`）表示。

返回映射之后形成的新字符串。

题目数据保证映射始终唯一。

---

示例 1：

```txt
输入：s = "10#11#12"
输出："jkab"
```

解释：

- `"j"` -> `"10#"`
- `"k"` -> `"11#"`
- `"a"` -> `"1"`
- `"b"` -> `"2"`

---

示例 2：

```txt
输入：s = "1326#"
输出："acz"
```

---

提示：

- `1 <= s.length <= 1000`
- `s[i]` 只包含数字（`'0'` - `'9'`）和 `'#'` 字符。
- `s` 是映射始终存在的有效字符串。

## 2. 🎯 s.1 - 逆向扫描解码

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(L)$，$L$ 为字符串长度
- 空间复杂度：$O(L)$，输出字符串长度

算法思路：

- 从右向左扫描，遇到 `#` 取其前两位组成两位数；否则取当前一位数
- 将数字映射到字符 `String.fromCharCode(96 + num)`，追加到结果数组，最后反转拼接
