---
id: 9e4b56c0-b204-4f47-b8f1-9ed3e2875009
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/harshad-number/)

如果一个整数能够被其各个数位上的数字之和整除，则称之为 哈沙德数（Harshad number）。给你一个整数 `x`。如果 `x` 是 哈沙德数，则返回 `x` 各个数位上的数字之和，否则，返回 `-1`。

---

示例 1：

```txt
输入：x = 18
输出：9
```

解释：

- `x` 各个数位上的数字之和为 `9`。`18` 能被 `9` 整除。
- 因此 `18` 是哈沙德数，答案是 `9`。

---

示例 2：

```txt
输入：x = 23
输出：-1
```

解释：

- `x` 各个数位上的数字之和为 `5`。`23` 不能被 `5` 整除。
- 因此 `23` 不是哈沙德数，答案是 `-1`。

---

提示：

- `1 <= x <= 100`

## 2. 🎯 s.1 - 模拟

::: code-group

```js [js]
/**
 * @param {number} x
 * @return {number}
 */
var sumOfTheDigitsOfHarshadNumber = function (x) {
  // 计算各数位之和
  let sum = 0
  let num = x
  while (num > 0) {
    sum += num % 10
    num = Math.floor(num / 10)
  }
  // 判断是否为哈沙德数
  return x % sum === 0 ? sum : -1
}
```

:::

- 时间复杂度：$O(\log x)$，计算数位和的时间
- 空间复杂度：$O(1)$，只使用了常数级别的额外空间

算法思路：

- 先计算 x 各个数位的数字之和 sum
- 判断 x 是否能被 sum 整除
- 如果能整除，返回 sum；否则返回 -1
