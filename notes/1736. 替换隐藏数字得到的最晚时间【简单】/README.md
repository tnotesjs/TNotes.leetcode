# [1736. 替换隐藏数字得到的最晚时间【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/1736.%20%E6%9B%BF%E6%8D%A2%E9%9A%90%E8%97%8F%E6%95%B0%E5%AD%97%E5%BE%97%E5%88%B0%E7%9A%84%E6%9C%80%E6%99%9A%E6%97%B6%E9%97%B4%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 贪心补全时间](#2--s1---贪心补全时间)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/latest-time-by-replacing-hidden-digits/)

给你一个字符串 `time`，格式为 `hh:mm`（小时：分钟），其中某几位数字被隐藏（用 `?` 表示）。

有效的时间为 `00:00` 到 `23:59` 之间的所有时间，包括 `00:00` 和 `23:59`。

替换 `time` 中隐藏的数字，返回你可以得到的最晚有效时间。

---

示例 1：

```txt
输入：time = "2?:?0"
输出："23:50"

解释：
以数字 '2' 开头的最晚一小时是 23，以 '0' 结尾的最晚一分钟是 50。
```

示例 2：

```txt
输入：time = "0?:3?"
输出："09:39"
```

示例 3：

```txt
输入：time = "1?:22"
输出："19:22"
```

---

提示：

- `time` 的格式为 `hh:mm`
- 题目数据保证你可以由输入的字符串生成有效的时间

## 2. 🎯 s.1 - 贪心补全时间

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(1)$，时间字符串长度固定为 5
- 空间复杂度：$O(1)$，使用固定大小的数组

算法思路：

- 将时间字符串转换为字符数组 `t`，逐位贪心填充最大合法数字
- 小时十位（`t[0]`）：如果个位是 `'?'` 或 `<= '3'`，填 `'2'`，否则填 `'1'`
- 小时个位（`t[1]`）：如果十位是 `'2'`，最多填 `'3'`，否则填 `'9'`
- 分钟十位（`t[3]`）：最多填 `'5'`
- 分钟个位（`t[4]`）：最多填 `'9'`
- 将字符数组拼接成字符串并返回
