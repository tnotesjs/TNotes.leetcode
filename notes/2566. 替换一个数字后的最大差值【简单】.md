---
id: 6897f785-a64e-4157-895c-5e7a4ea1973d
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/maximum-difference-by-remapping-a-digit/)

给你一个整数 `num`。你知道 Danny Mittal 会偷偷将 `0` 到 `9` 中的一个数字替换成另一个数字。

请你返回将 `num` 中恰好一个数字进行替换后，得到的最大值和最小值的差为多少。

注意：

- 当 Danny 将一个数字 `d1` 替换成另一个数字 `d2` 时，Danny 需要将 `num` 中所有 `d1` 都替换成 `d2`。
- Danny 可以将一个数字替换成它自己，也就是说 `num` 可以不变。
- Danny 可以将数字分别替换成两个不同的数字分别得到最大值和最小值。
- 替换后得到的数字可以包含前导 0。
- Danny Mittal 获得周赛 326 前 10 名，让我们恭喜他。

---

示例 1：

```txt
输入：num = 11891
输出：99009

解释：
为了得到最大值，我们将数字 1 替换成数字 9，得到 99899。
为了得到最小值，我们将数字 1 替换成数字 0，得到 890。
两个数字的差值为 99009。
```

示例 2：

```txt
输入：num = 90
输出：99

解释：
可以得到的最大值是 99（将 0 替换成 9），最小值是 0（将 9 替换成 0）。
所以我们得到 99。
```

---

提示：

- `1 <= num <= 10^8`

## 2. 🎯 s.1 - 贪心

::: code-group

```js [js]
/**
 * @param {number} num
 * @return {number}
 */
var minMaxDifference = function (num) {
  const str = String(num)

  // 求最大值：找第一个不是9的数字，替换成9
  let maxStr = str
  for (let i = 0; i < str.length; i++) {
    if (str[i] !== '9') {
      maxStr = str.replaceAll(str[i], '9')
      break
    }
  }

  // 求最小值：替换第一个数字使其尽可能小
  let minStr = str
  const firstDigit = str[0]
  if (firstDigit !== '0') {
    minStr = str.replaceAll(firstDigit, '0')
  }

  return Number(maxStr) - Number(minStr)
}
```

:::

- 时间复杂度：$O(L)$，其中 L 是数字的位数，需要遍历字符串进行替换
- 空间复杂度：$O(L)$，字符串操作所需空间

算法思路：

- 求最大值：找到从左到右第一个不是 9 的数字，将其全部替换为 9
- 求最小值：将第一个数字全部替换为 0（这样可以保证最小，允许前导 0）
- 计算最大值与最小值的差值并返回
