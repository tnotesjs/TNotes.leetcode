---
id: 2301ea3c-8bd3-4875-84d6-bfb4c6b44216
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/can-make-arithmetic-progression-from-sequence/)

给你一个数字数组 `arr`。

如果一个数列中，任意相邻两项的差总等于同一个常数，那么这个数列就称为等差数列。

如果可以重新排列数组形成等差数列，请返回 `true`；否则，返回 `false`。

---

示例 1：

```txt
输入：arr = [3,5,1]
输出：true

解释：
对数组重新排序得到 [1,3,5] 或者 [5,3,1]，
任意相邻两项的差分别为 2 或 -2，可以形成等差数列。
```

---

示例 2：

```txt
输入：arr = [1,2,4]
输出：false

解释：
无法通过重新排序得到等差数列。
```

---

提示：

- `2 <= arr.length <= 1000`
- `-10^6 <= arr[i] <= 10^6`

## 2. 🎯 s.1 - 排序验差

::: code-group

```js [js]
/**
 * @param {number[]} arr
 * @return {boolean}
 */
var canMakeArithmeticProgression = function (arr) {
  // 排序
  arr.sort((a, b) => a - b)

  // 取公差
  const diff = arr[1] - arr[0]

  // 验公差
  for (let i = 2; i < arr.length; i += 1) {
    if (arr[i] - arr[i - 1] !== diff) return false
  }

  return true
}
```

:::

- 时间复杂度：$O(N\log N)$，排序后单次遍历
- 空间复杂度：$O(1)$，原地排序（忽略输出）

算法思路：

- 排序后相邻差必须全相等，先取前两个差值作为公差
- 遍历其余相邻对，若有差值不等则返回 false，否则 true
