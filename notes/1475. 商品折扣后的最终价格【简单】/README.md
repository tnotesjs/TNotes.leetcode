# [1475. 商品折扣后的最终价格【简单】](https://github.com/tnotesjs/TNotes.leetcode/tree/main/notes/1475.%20%E5%95%86%E5%93%81%E6%8A%98%E6%89%A3%E5%90%8E%E7%9A%84%E6%9C%80%E7%BB%88%E4%BB%B7%E6%A0%BC%E3%80%90%E7%AE%80%E5%8D%95%E3%80%91)

<!-- region:toc -->

- [1. 📝 题目描述](#1--题目描述)
- [2. 🎯 s.1 - 暴力解法](#2--s1---暴力解法)
- [3. 🎯 s.2 - 单调栈](#3--s2---单调栈)

<!-- endregion:toc -->

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/final-prices-with-a-special-discount-in-a-shop/)

给你一个数组 `prices`，其中 `prices[i]` 是商店里第 `i` 件商品的价格。

商店里正在进行促销活动，如果你要买第 `i` 件商品，那么你可以得到与 `prices[j]` 相等的折扣，其中 `j` 是满足 `j > i` 且 `prices[j] <= prices[i]` 的最小下标，如果没有满足条件的 `j`，你将没有任何折扣。

请你返回一个数组，数组中第 `i` 个元素是折扣后你购买商品 `i` 最终需要支付的价格。

---

示例 1：

```txt
输入：prices = [8,4,6,2,3]
输出：[4,2,4,2,3]
```

解释：

- 商品 0 的价格为 `price[0]=8`，你将得到 `prices[1]=4` 的折扣，所以最终价格为 `8 - 4 = 4`。
- 商品 1 的价格为 `price[1]=4`，你将得到 `prices[3]=2` 的折扣，所以最终价格为 `4 - 2 = 2`。
- 商品 2 的价格为 `price[2]=6`，你将得到 `prices[3]=2` 的折扣，所以最终价格为 `6 - 2 = 4`。
- 商品 3 和 4 都没有折扣。

---

示例 2：

```txt
输入：prices = [1,2,3,4,5]
输出：[1,2,3,4,5]

解释：
在这个例子中，所有商品都没有折扣。
```

---

示例 3：

```txt
输入：prices = [10,1,1,6]
输出：[9,0,1,6]
```

---

提示：

- `1 <= prices.length <= 500`
- `1 <= prices[i] <= 10^3`

## 2. 🎯 s.1 - 暴力解法

::: code-group

<<< ./solutions/1/1.js [js]

:::

- 时间复杂度：$O(n^2)$，对每个商品向右线性查找首个不大于当前价的折扣
- 空间复杂度：$O(1)$，仅使用常数变量

算法思路：

- 枚举每个位置 $i$，向右查找第一个价格不大于 `prices[i]` 的位置 $j$
- 若找到则折扣为 `prices[j]`，否则折扣为 `0`
- 最终价格为 `prices[i] - discount`，逐个填入结果

## 3. 🎯 s.2 - 单调栈

::: code-group

<<< ./solutions/2/1.js [js]

:::

- 时间复杂度：$O(n)$，每个元素入栈出栈至多一次
- 空间复杂度：$O(n)$，单调栈存储候选折扣价

算法思路：

- ← 从右向左遍历，维护单调非降栈，栈顶是离当前最近且不大于当前价的折扣
- 若栈顶元素大于当前价，意味着这个栈顶元素不可能作为折扣，栈顶弹出，直到栈顶不大于当前价或栈空
  - 🤔 为什么若栈顶元素大于当前价，意味着这个栈顶元素不可能作为折扣？
  - 栈顶元素价格更高，前面的元素在匹配折扣时，若能匹配栈顶价，必然能匹配当前价，题目要求下标尽可能小，因此位于栈顶价左侧的当前价必然优先被匹配
- 当前商品的折扣即栈顶（不存在则为 0），计算后将当前价入栈
