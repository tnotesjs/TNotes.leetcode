---
id: c74fa32d-232b-468b-ab95-4cec4261ef8a
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/find-a-corresponding-node-of-a-binary-tree-in-a-clone-of-that-tree/)

给你两棵二叉树，原始树 `original` 和克隆树 `cloned`，以及一个位于原始树 `original` 中的目标节点 `target`。

其中，克隆树 `cloned` 是原始树 `original` 的一个副本。

请找出在树 `cloned` 中，与 `target` 相同的节点，并返回对该节点的引用（在 C/C++ 等有指针的语言中返回节点指针，其他语言返回节点本身）。

---

注意：你不能更改两棵二叉树，以及 `target` 节点。只能返回对克隆树 `cloned` 中已有的节点的引用。

---

示例 1：

![img](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-09-20-18-57-07.png)

```txt
输入: tree = [7,4,3,null,null,6,19], target = 3
输出: 3

解释:
上图画出了树 original 和 cloned。
target 节点在树 original 中，用绿色标记。
答案是树 cloned 中的黄颜色的节点（其他示例类似）。
```

---

示例 2：

![img](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-09-20-18-57-24.png)

```txt
输入: tree = [7], target =  7
输出: 7
```

---

示例 3：

![img](https://cdn.jsdelivr.net/gh/tnotesjs/imgs@main/2025-09-20-18-57-29.png)

```txt
输入: tree = [8,null,6,null,5,null,4,null,3,null,2,null,1], target = 4
输出: 4
```

---

提示：

- 树中节点的数量范围为 `[1, 10^4]`。
- 同一棵树中，没有值相同的节点。
- `target` 节点是树 `original` 中的一个节点，并且不会是 `null`。

---

进阶：如果树中允许出现值相同的节点，将如何解答？

## 2. 🎯 s.1 - 栈遍历

::: code-group

```js [js]
/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} original
 * @param {TreeNode} cloned
 * @param {TreeNode} target
 * @return {TreeNode}
 */
var getTargetCopy = function (original, cloned, target) {
  const stack = [[original, cloned]]

  // 同步遍历原树与克隆树，找到目标节点对应位置
  while (stack.length) {
    const [o, c] = stack.pop()
    if (o === target) return c

    if (o.right) stack.push([o.right, c.right])
    if (o.left) stack.push([o.left, c.left])
  }

  return null
}
```

:::

- 时间复杂度：$O(n)$，遍历整棵树
- 空间复杂度：$O(n)$，迭代栈最坏装下整层节点

算法思路：

- 用栈同步存放原树与克隆树的对应节点对
- 弹出一对节点，若原树节点即为 `target`，返回克隆树节点
- 否则将左右子节点成对压栈继续遍历

## 3. 🎯 s.2 - 递归遍历

::: code-group

```js [js]
/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} original
 * @param {TreeNode} cloned
 * @param {TreeNode} target
 * @return {TreeNode}
 */
var getTargetCopy = function (original, cloned, target) {
  // 如果原始树为空，返回null
  if (!original) {
    return null
  }

  // 如果当前节点就是目标节点，返回克隆树中的对应节点
  if (original === target) {
    return cloned
  }

  // 递归搜索左子树
  const leftResult = getTargetCopy(original.left, cloned.left, target)
  if (leftResult) {
    return leftResult
  }

  // 递归搜索右子树
  return getTargetCopy(original.right, cloned.right, target)
}
```

:::

- 时间复杂度：$O(n)$，其中 $n$ 为节点总数，最坏情况下需遍历所有节点
- 空间复杂度：$O(h)$，其中 $h$ 为树的高度，递归深度受限于树高

算法思路：

- 若当前节点即为 `target`，返回对应的克隆节点 `cloned`
- 先递归搜索左子树，若找到目标则直接返回
- 否则继续递归搜索右子树并返回结果
