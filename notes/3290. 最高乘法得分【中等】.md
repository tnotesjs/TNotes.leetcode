---
id: def1c527-07cd-4e1c-aac4-0238d3d96215
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/maximum-multiplication-score/)

给你一个大小为 4 的整数数组 `a` 和一个大小 至少为 4 的整数数组 `b`。

你需要从数组 `b` 中选择四个下标 `i0`, `i1`, `i2`, 和 `i3`，并满足 `i0 < i1 < i2 < i3`。你的得分将是 `a[0] * b[i0] + a[1] * b[i1] + a[2] * b[i2] + a[3] * b[i3]` 的值。

返回你能够获得的 最大 得分。

---

示例 1：

输入：a = [3,2,5,6], b = [2,-6,4,-5,-3,2,-7]

输出：26

解释：

选择下标 0, 1, 2 和 5。得分为 `3 * 2 + 2 * (-6) + 5 * 4 + 6 * 2 = 26`。

---

示例 2：

输入：a = [-1,4,5,-2], b = [-5,-1,-3,-2,-4]

输出：-1

解释：

选择下标 0, 1, 3 和 4。得分为 `(-1) * (-5) + 4 * (-1) + 5 * (-2) + (-2) * (-4) = -1`。

---

提示：

- `a.length == 4`
- `4 <= b.length <= 10^5`
- `-10^5 <= a[i], b[i] <= 10^5`

## 2. 🎯 s.1 - 动态规划

::: code-group

```js [js]
/**
 * @param {number[]} a
 * @param {number[]} b
 * @return {number}
 */
var maxScore = function(a, b) {
  const n = b.length
  // dp[j][k] = 用 b 的前 j 个元素选了 k 个（匹配 a[0..k-1]）的最大得分
  // dp[j][k] = max(dp[j-1][k], dp[j-1][k-1] + a[k-1] * b[j-1])
  let dp = [-Infinity, -Infinity, -Infinity, -Infinity, 0]
  // dp[k] = 选了 k 个的最大得分 (k=0..4)
  let prev = [0, -Infinity, -Infinity, -Infinity, -Infinity]
  for (let j = 0; j < n; j++) {
    const curr = prev.slice()
    for (let k = 1; k <= 4; k++) {
      curr[k] = Math.max(curr[k], prev[k - 1] + a[k - 1] * b[j])
    }
    prev = curr
  }
  return prev[4]
}
```

:::

- 时间复杂度：$O(4n)$，遍历数组 b
- 空间复杂度：$O(1)$，只用常数额外空间

算法思路：

- dp[k] 表示从 b 中选了 k 个元素匹配 a[0..k-1] 的最大乘积和
- 对 b 的每个元素，倒序更新 dp[k] = max(dp[k], dp[k-1] + a[k-1] * b[j])
