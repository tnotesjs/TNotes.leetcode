---
id: 6271c6f7-72e2-4230-a24b-8526ea21da29
---

## 1. 📝 题目描述

- [leetcode](https://leetcode.cn/problems/sum-multiples/)

给你一个正整数 `n`，请你计算在 `[1，n]` 范围内能被 `3`、`5`、`7` 整除的所有整数之和。

返回一个整数，用于表示给定范围内所有满足约束条件的数字之和。

---

示例 1：

```txt
输入：n = 7
输出：21

解释：
在 [1, 7] 范围内能被 3、5、7 整除的所有整数分别是 3、5、6、7。
数字之和为 21。
```

---

示例 2：

```txt
输入：n = 10
输出：40

解释：
在 [1, 10] 范围内能被 3、5、7 整除的所有整数分别是 3、5、6、7、9、10。
数字之和为 40。
```

---

示例 3：

```txt
输入：n = 9
输出：30

解释：
在 [1, 9] 范围内能被 3、5、7 整除的所有整数分别是 3、5、6、7、9。
数字之和为 30。
```

---

提示：

- 1 <= n <= 10^3

## 2. 🎯 s.1 - 遍历

::: code-group

```js [js]
/**
 * @param {number} n
 * @return {number}
 */
var sumOfMultiples = function (n) {
  let sum = 0

  for (let i = 1; i <= n; i++) {
    // 检查是否能被 3、5 或 7 整除
    if (i % 3 === 0 || i % 5 === 0 || i % 7 === 0) {
      sum += i
    }
  }

  return sum
}
```

:::

- 时间复杂度：$O(N)$，需要遍历 1 到 n 的所有数字
- 空间复杂度：$O(1)$，只使用常数额外空间

算法思路：

- 遍历从 1 到 n 的所有数字
- 检查每个数字是否能被 3、5 或 7 整除
- 如果能被整除，将该数字累加到结果中
- 返回最终的总和
