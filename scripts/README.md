# scripts

下面的表格只统计当前 `scripts/` 目录下可执行的 `.js` 脚本文件。

| 脚本                           | 简介                                                           |
| ------------------------------ | -------------------------------------------------------------- |
| `format-check/format-check.js` | 检查 `notes/` 与 `scripts/` 下的文件是否符合仓库约定的格式规则 |
